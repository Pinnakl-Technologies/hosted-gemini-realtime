[service]
root = "."
# Match the Dockerfile CMD for consistency
start = "sh -c 'cd src && python agent.py dev & cd web && npm run dev'"
healthcheck = ""
restart_policy = "on-failure"
max_restart_retries = 10
