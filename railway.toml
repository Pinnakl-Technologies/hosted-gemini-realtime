[service]
root = "."
start = "bash -c '/opt/venv/bin/python src/agent.py dev & cd web && npx next start --hostname 0.0.0.0 --port 3000'"
healthcheck = ""  # Disable healthcheck completely
restart_policy = "on-failure"
max_restart_retries = 10
