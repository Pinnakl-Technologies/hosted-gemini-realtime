[service]
root = "."
# Parallel execution of agent and web frontend
start = "sh -c 'python src/agent.py dev & cd web && npm run dev:web'"
healthcheck = ""
restart_policy = "on-failure"
max_restart_retries = 10
